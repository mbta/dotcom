import {
  blueLineIcon,
  busIcon,
  commuterRailIcon,
  ferryIcon,
  genericSubwayIcon,
  greenLineIcon,
  greenBLineIcon,
  greenCLineIcon,
  greenDLineIcon,
  greenELineIcon,
  mattapanLineIcon,
  orangeLineIcon,
  redLineIcon,
  silverLineIcon,
  alertIcon,
  vehicleArrowIcon,
  searchIcon,
  crowdingIcon
} from "../../helpers/icon";

describe("blueLineIcon", () => {
  it("works with a default className", () => {
    const result = blueLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("busIcon", () => {
  it("works with a default className", () => {
    const result = busIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("commuterRailIcon", () => {
  it("works with a default className", () => {
    const result = commuterRailIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("ferryIcon", () => {
  it("works with a default className", () => {
    const result = ferryIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("genericSubwayIcon", () => {
  it("works with a default className", () => {
    const result = genericSubwayIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("greenLineIcon", () => {
  it("works with a default className", () => {
    const result = greenLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("greenBLineIcon", () => {
  it("works with a default className", () => {
    const result = greenBLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("greenCLineIcon", () => {
  it("works with a default className", () => {
    const result = greenCLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("greenDLineIcon", () => {
  it("works with a default className", () => {
    const result = greenDLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("greenELineIcon", () => {
  it("works with a default className", () => {
    const result = greenELineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("mattapanLineIcon", () => {
  it("works with a default className", () => {
    const result = mattapanLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("orangeLineIcon", () => {
  it("works with a default className", () => {
    const result = orangeLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("redLineIcon", () => {
  it("works with a default className", () => {
    const result = redLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("silverLineIcon", () => {
  it("works with a default className", () => {
    const result = silverLineIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("alertIcon", () => {
  it("works with a default className", () => {
    const result = alertIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("vehicleArrowIcon", () => {
  it("works with a default className", () => {
    const result = vehicleArrowIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("searchIcon", () => {
  it("works with a default className", () => {
    const result = searchIcon();
    expect(result.props.className).toEqual("notranslate");
  });
});

describe("crowdingIcon", () => {
  it("works with a default className", () => {
    const result = crowdingIcon();
    expect(result.props.className).toEqual(
      "notranslate c-svg__icon c-icon__crowding "
    );
  });
});
