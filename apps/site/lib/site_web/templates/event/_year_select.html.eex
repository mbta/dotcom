<select class="c-select m-event-list__select">
  <option disabled selected value="">Jump to</option>
  <%= for year <- years_for_selection(@conn) do %>
    <%= content_tag(
      :option,
      year,
      [
        value: event_path(@conn, :index, month: 1, year: year),
        selected: year == @year
      ]
    ) %>
  <% end %>
</select>
